# Claude Code instructions

<!-- Claude Code reads CLAUDE.md and not AGENTS.md, so this file imports it
     instead of duplicating the instructions: both read the same source. An
     import rather than a symlink, because a symlink needs Administrator rights
     or Developer Mode on Windows. Add Claude-specific instructions below the
     import if you ever need any. -->

@AGENTS.md
